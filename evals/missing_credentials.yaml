name: proofpoint_dlp_list_incidents returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: proofpoint_dlp_list_incidents
  args: {}
  expect:
    is_error: true
timeout: 30s
